# 업로드 폴더에서는 스크립트 실행 금지 (이미지만 서빙)
<FilesMatch "\.(php|phtml|php[0-9]|phar|cgi|pl|py|sh)$">
  <IfModule mod_authz_core.c>
    Require all denied
  </IfModule>
  <IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
  </IfModule>
</FilesMatch>
