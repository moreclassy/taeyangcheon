# 브라우저에서 직접 열 수 있는 파일은 api.php 만
<FilesMatch "^(config|config\.sample|lib|seed)\.php$">
  <IfModule mod_authz_core.c>
    Require all denied
  </IfModule>
  <IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
  </IfModule>
</FilesMatch>
